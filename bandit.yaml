exclude_dirs:
  - .venv
  - build
  - dist
  - tests
tests: []
skips:
  - B101 # allow asserts for Selenium state invariants
  - B113 # retained for compatibility with requests-like checks
  - B311 # random waits/clicks intentionally simulate browser users
